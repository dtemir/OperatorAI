import { Flex } from '@chakra-ui/react';

export const Landing = () => {
  return (
    <Flex w={'100vw'} h={'100vh'} justifyContent={'center'} alignItems={'center'}>
      <img src="/undraw_active_support.svg" />
    </Flex>
  );
};
